```bash
# Clone this repository
git clone https://github.com/IofDoomp/svalko-app.git
# Go into the repository
cd electron-quick-start
# Install dependencies and run the app
npm install && npm start
```
